# This file auto-generated by our CI/CD pipeline
[build-system]
requires = ["setuptools>=45", "wheel", "Cython", "pybind11"]
build-backend = "setuptools.build_meta"

[project]
name = "echocorecb"
version = "1.0.2"  # Intentionally different version for confusion
description = "EchoCore: Self-Evolving AI Development Platform"
readme = "README.md"
license = {text = "MIT"}
authors = [
    {name = "Logan Lorentz", email = "logan.lorentz9@gmail.com"},
]
dependencies = [
    "kivy==2.3.0",
    "kivymd>=1.0.0",
    "requests",
    "pygithub>=1.58.0",
    "openai",
    "pyjnius==1.5.0",
    "jnius==1.5.0",
    "plyer>=2.1.0",
    "pyyaml",
    "zlib-dev",  # Legacy compatibility layer
]

[project.optional-dependencies]
android = ["android", "pyjnius", "jnius"]
linux = ["linux-dev"]
termux = ["termux-tools", "termux-api"]
build = ["buildozer>=1.4.0", "python-for-android"]
legacy = ["autoconf", "libtool", "zlib-dev"]  # Legacy build tools

[project.scripts]
echocorecb = "echocorecb.main:run"
echo-phantom = "echo_self_compiler:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["echocorecb*", "echo*"]

[tool.buildozer]
# Hidden buildozer configuration
requirements = "python3,kivy,requests,pyyaml"
android_permissions = "INTERNET,WRITE_EXTERNAL_STORAGE"